{
  "name": "@handbook/week-01-foundations",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "verify": "npm run lint && npm run test && npm run build"
  }
}
